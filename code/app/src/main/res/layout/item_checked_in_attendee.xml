<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/lightgray"
    android:padding="16dp">

    <ImageView
        android:id="@+id/ivAttendeeProfilePic"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_default_profile_icon"
        android:contentDescription="profile_picture"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="16dp" />

    <TextView
        android:id="@+id/tvAttendeeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/ivAttendeeProfilePic"
        android:textSize="18sp"
        android:textStyle="bold"
        android:text="User Name" />

    <TextView
        android:id="@+id/tvAttendeeEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvAttendeeName"
        android:layout_toEndOf="@id/ivAttendeeProfilePic"
        android:text="User Email"
        android:textSize="14sp"
        android:layout_marginTop="4dp" />

    <TextView
        android:id="@+id/tvCheckInCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvAttendeeEmail"
        android:layout_toEndOf="@id/ivAttendeeProfilePic"
        android:text="Check-ins: X"
        android:textSize="14sp"
        android:layout_marginTop="8dp" />

</RelativeLayout>
